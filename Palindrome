class Solution {
    public boolean isPalindrome(int x) {
       int y=x;
        int sum=0;
         while(x>0)
        {
            int r=x%10;
            sum=r+sum*10;
            x=x/10;
        }
        if(sum==y)
        return true;
        else
        return false;
    }
}
